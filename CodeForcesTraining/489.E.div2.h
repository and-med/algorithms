#include<iostream>
#include<vector>
using namespace std;

void doTask(istream& in, ostream& out)
{
	int n, q;
	in >> n >> q;
	vector<int> a(n);
	for (int i = 0; i < n; ++i) in >> a[i];

	vector<pair<int, int>> st(2 * n);


}